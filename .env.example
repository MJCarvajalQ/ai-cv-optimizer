# AI Provider Configuration
AI_PROVIDER=openai
OPENAI_MODEL=gpt-3.5-turbo

# Google Configuration
GOOGLE_SHEETS_ID=your_google_sheets_id_here
GOOGLE_CREDENTIALS_PATH=oauth_credentials.json

# CV Settings
BASE_CV_PATH=templates/base_cv_english.json
OUTPUT_FOLDER=output/generated_cvs

# Security Note: 
# OPENAI_API_KEY should be stored securely in macOS Keychain
# Run: security add-generic-password -a "$USER" -s "openai-api-key" -w "your-api-key" -T ""
